import random as a
p=input("password: ")
z,x,j=", yo ooned","""print(f'''it wants a password.
it's encrypted.
{p}''')""","".join
a.seed(p)
if j(a.sample(p,len(z)))==z:
 d=list(range(len(x)))
 a.shuffle(d)
 exec(j(x[i]for i in d))